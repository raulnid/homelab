apiVersion: v1
kind: Service
metadata:
  name: wireguard
  namespace: wireguard
spec:
  type: LoadBalancer
  loadBalancerIP: ${WG_SERVICE_IP}
  ports:
    - port: 51822
      targetPort: 51822
      protocol: UDP
      name: wireguard
  selector:
    app: wireguard
