apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mealie
  namespace: mealie
spec:
  entryPoints:
    - websecure
  routes:
  - match: Host(`mealie.home.lab`)
    kind: Rule
    services:
    - name: mealie
      port: 80
  tls:
    certResolver: default
